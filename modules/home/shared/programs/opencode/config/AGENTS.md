# Global OpenCode Instructions

A `chat.params` plugin (`~/.config/opencode/plugins/forced-eval.ts`) injects per-response skill evaluation instructions into every system prompt.
Do not override or ignore these injected instructions.
